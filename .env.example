# Root example values for Docker/local reference.
# Copy service-specific values into each app if running without Docker.

POSTGRES_DB=ship_dashboard
POSTGRES_USER=ship_admin
POSTGRES_PASSWORD=ship_password
DATABASE_URL=postgres://ship_admin:ship_password@localhost:5432/ship_dashboard
BACKEND_PORT=8000
AI_SERVICE_URL=http://localhost:8001
VITE_API_URL=http://localhost:8000
